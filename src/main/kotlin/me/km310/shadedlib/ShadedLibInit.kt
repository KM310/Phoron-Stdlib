package me.km310.shadedlib

import net.fabricmc.api.ModInitializer

object ShadedLibInit : ModInitializer {
    override fun onInitialize() {
        // absichtlicher no-op: nur damit die Lib als Mod existiert
    }
}
